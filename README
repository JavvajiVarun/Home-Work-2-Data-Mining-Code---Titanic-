# Titanic Survival Prediction - DM Models 1

This project implements:
- Data preprocessing (handling missing values, encoding)
- Decision Tree (fine-tuned with GridSearchCV)
- Random Forest (fine-tuned with GridSearchCV)
- 5-fold cross-validation for both models

Results:
- Decision Tree Accuracy: ~0.827
- Random Forest Accuracy: ~0.833

Conclusion:
Random Forest performs slightly better due to reduced overfitting.


Random Forest performs slightly better than Decision Tree, with an average accuracy of 0.833 compared to 0.827. 
This improvement is expected because Random Forest reduces overfitting by combining multiple trees,whereas a single Decision Tree is more prone to variance. 
Therefore, Random Forest is the better model for this task.
